import React from 'react';
import HeaderBanner from './HeaderBanner';
import Nav from './Nav';

const Header = () => {
  
    return (

        <header>
            <Nav />
            <HeaderBanner />
        </header>

    );};

export default Header;